#!/bin/sh

set -e

_repo=$1
if [ -z "$_repo" ]; then
  echo "You must specify a repository to clone"
  exit 129 # Mimic `git clone` exit code
fi

_path=$(echo "$_repo" | sed -E "s/^(https:\/\/|git@)//; s/:/\//; s/.git$//")
CODE_PATH=${CODE_PATH:-$HOME/Code}
git clone "$_repo" "$CODE_PATH/$_path"
