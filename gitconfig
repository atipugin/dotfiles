[user]
email = atipugin@gmail.com
name = Alexander Tipugin

[core]
autocrlf = input
excludesfile = ~/.gitignore_global

[color]
ui = auto

[push]
default = current

[credential]
helper = osxkeychain

[branch]
autosetuprebase = always

[fetch]
prune = true

[merge]
ff = only

[alias]
it = "!git init && git commit -m 'init' --allow-empty"
please = push --force-with-lease
